with open('nums.txt') as numlist:
    numbers =  numlist.readlines()


print(numbers)
print(numbers[0].strip())

for x in range(10):
    answer = ""
    for num in numbers:
        answer += chr(int(num) + x)
    print(answer)
